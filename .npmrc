# vitest@5 声明了 12 个 optional peerDependencies（含 webdriverio 等带传递 peer 环的包），
# 其深层 peer 图会触发 npm 10.9.2 arborist 的 #loadPeerSet 崩溃：
#   TypeError: Cannot read properties of null (reading 'edgesOut')
# 关闭自动 peer 解析以保证 npm install / npm ci 在本机与 CI 中行为一致。
#
# 代价：peer 冲突不再自动拦截，需人工核对关键 peer 范围。已核对项：
#   - typescript-eslint@8.69.0 要求 typescript >=4.8.4 <6.1.0  → 本项目 typescript ~5.9.3 ✅
#     （TypeScript 7.0.2 超出范围，故未采用）
#   - vitest@5.0.0 要求 @types/node ^22.0.0 || >=24.0.0        → 本项目 @types/node ^26.4.1 ✅
#   - vitest@5.0.0 要求 vite ^6.4.0 || ^7.0.0 || ^8.0.0        → 本项目 vite ^8.2.2 ✅
legacy-peer-deps=true
